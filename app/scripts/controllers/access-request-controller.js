'use strict';

tsdApp
  .controller('AccessRequestController', ['$cookies','$scope',function($cookies, $scope) {

      $scope.send = function() {
      };

      $scope.cancel = function() {
      };

}]);
